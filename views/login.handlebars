<main class="has-background-primary-light">
    <div class="container is-bordered px-4 m-4" id="login-form">
    <table class="table is-bordered is-fullwidth ">
        <form class="form" id = "login-form">
        <div class="notification is-dark">
            <h2 class="is-size-4 has-text-weight-bold">Login</h2>
        </div>
        <tbody>
            <tr>
                <h4>Username</h4>
            </tr>
            <tr>
                <input class="input" id ="username-login" type="text" value="">
                <span class="icon is-small is-right">
                    <i class="fas fa-exclamation-triangle"></i>
                </span>
            </tr>

            <tr>
                <h4>Password</h4>
            </tr>
            <tr>
                <input class="input" id="password-login" type="password" value="">
                <span class="icon is-small is-right">
                    <i class="fas fa-envelope"></i>
                </span>
            </tr>
            <tr>
                <div class="field">
                    <div class="control">
                        <button class="button is-light button is-fullwidth ">Login!</button>
                    </div>
                </div>
            </tr>
            <tfoot>
                <p><a class=" href='signup'">Sign up</a> instead </p>
            </tfoot>

        </tbody>
        </form>
    </table>
    </div>
</main>


<script src="/js/login.js"></script>